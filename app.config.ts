export default defineAppConfig({
   ui: {
      skeleton: {
         base: "animate-pulse rounded-md bg-elevated",
      },
      container: {
         base: "w-full max-w-(--ui-container)  px-2",
      },
   },
});
